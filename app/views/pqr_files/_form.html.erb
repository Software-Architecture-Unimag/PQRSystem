<%= form_with(model: pqr_file, local: true) do |form| %>
  <% if pqr_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pqr_file.errors.count, "error") %> prohibited this pqr_file from being saved:</h2>

      <ul>
      <% pqr_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
